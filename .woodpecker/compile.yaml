when:
  - event: push
    branch: [main, develop]

steps:
  - name: Build
    image: mcr.microsoft.com/dotnet/sdk:8.0
    commands:
      - dotnet --version
      - dotnet restore
    # volumes:
    #   - /mnt/user/caddy-srv:/mnt/caddy-srv